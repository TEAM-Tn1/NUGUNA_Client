enum uri {
  oauth = '/users/oauth',
  signin = '/users/auth',
  info = '/users/info',
  alarm = '/users/template',
  feed = '/feed',
  carrot = '/feed/carrot',
  group = '/feed/group',
  photo = '/feed/photo',
  report = '/feed/report',
  like = '/feed/like',
  tag = '/feed/tag',
}

export default uri;
